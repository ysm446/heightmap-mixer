# Changelog

作成日時: 2026-08-31 06:18
更新日時: 2026-09-01 04:10

## 未リリース

### 2026-09-01 04:10 — 影とテセレーション

- **影を落とせるようにした**（`ライティングと露出 > 影`、既定でオン）。
  ディレクショナルライトから 2048^2 のシャドウマップを描いて落とす。
  **ディスプレイスメントで押し出した形にもそのまま落ちる**ので、
  凹凸の見え方が一段と分かりやすくなる。3x3 のソフトシャドウ。
- **テセレーションを追加した**（`プレビュー設定 > テセレーション`、既定でオフ）。
  画面上の辺が長いところだけ細かく割るので、**寄ったときだけ**分割が増える。
  `分割の上限` で 1 辺あたりの分割数の上限を決める。
- どちらもプロジェクトに保存される。

### 2026-09-01 02:48 — 表示モードをビューポート内へ、ワイヤーフレーム、右カラムのタブ化

- **表示モードの切り替えをビューポートの左上に置いた**（`シェーディング v` のボタン）。
  トップメニューからは外した。見ている場所から目を離さずに切り替えられ、
  いまどの表示なのかも常に見える。既定以外のときは文字が警告色になる。
- **ワイヤーフレーム表示を追加した。** メッシュの分割とディスプレイスメントの効き方を、
  形だけで確認できる。
- **右カラム（プレビュー設定 / ライティングと露出 / 情報）をタブにした。**
  縦に積むと 1 枚あたりが短く、どれもスクロールしないと全体が見えなかった。
- 既定レイアウトの前面タブを明示するようにした（レイヤー / プレビュー設定）。

### 2026-09-01 02:05 — ディスプレイスメント（ハイトを形状に反映）

- **プレビュー設定に `変位量` を追加した。** 合成したハイトで実際にメッシュを押し出す。
  0 なら形は変わらない（既定）。シルエットも変わるので、凹凸の出方を形で確認できる。
- 押し出しは頂点シェーダで行う。細かさを出すためプレビューのメッシュを細かくした
  （球 256x128 / 平面 256 分割 / キューブは 1 面 96 分割）。
- 法線は合成した Normal をそのまま使う（もともとハイトの勾配から作っている）。
  変位量を大きくすると凹凸が形と法線で二重に効くので、
  レイヤーの `法線の強さ` を下げると自然になる。
- 変位量はプロジェクトに保存される。

### 2026-09-01 01:05 — ビューポートの表示モード

- **`表示 > ビューポートに表示` を追加した。** 合成したチャンネルをそのまま見られる。
  シェーディング（既定）/ ベースカラー / 法線（接空間）/ 法線（ワールド）/
  ラフネス / メタルネス / AO / ハイト。
  法線は「マップそのもの（接空間）」と「陰影に使う向き（ワールド）」の 2 つを選べる。
- 値を確かめるための表示なので、露出もトーンマップも掛けず、背景も描かない。
  ベースカラーだけは sRGB へ直して出す。
- 既定以外の表示になっているときは、ステータスバーに `表示: 法線` のように出る。
- ライトの**仰角を -89 度まで**下げられるようにした（下からの光も試せる）。

### 2026-09-01 00:22 — L + ドラッグでライトの向きを変える

- **ビューポートで `L` を押しながら左ドラッグすると、ライトの向きが変わる。**
  横が方位角、縦が仰角（0.25 度 / ピクセル）。パネルのスライダーを往復せずに、
  見ながら光を回せる。
- 動かしている間はビューポートにギズモを重ねる。地面のリング、水平方向、仰角の弧、
  光が来る向きの矢印、そしていまの角度。離すと 0.35 秒で消える。
- L + 左ドラッグはブラシや視点の回転より優先する。

### 2026-08-31 23:58 — きりのいい値を入れやすくした

- **UV スケールなど、値を狙って合わせたい行をドラッグすると刻みへ吸着する。**
  UV スケールは 0.25、ノイズの周波数とオフセットは 0.5、焦点距離は 1mm 刻み。
  2.00 や 4.00 をドラッグだけで出せる。
- **数値の行はどれも Ctrl + クリックで直接入力できる**（前からできたが気づけなかった）。
  ツールチップにその旨を出すようにした。直接入力した値は刻みで丸めない。
- 0〜1 の比率（ラフネスなど）は刻まない。中間の値そのものに意味があるため。

### 2026-08-31 23:14 — 設定ウィンドウと UI の拡大率

- **`表示 > 設定` を追加した。** プロジェクトに保存しないアプリ側の設定を置く。
  設定は `%LOCALAPPDATA%/material-mixer/settings.json` に残り、次の起動でも効く。
- **UI の拡大率を選べるようにした。**
  「スケール追従」を入れると Windows の表示スケール（150% など）に合わせて
  文字と部品が大きくなる。切ると拡大率を 100 / 125 / 150 / 200% から選べる。
  **これまでは常に 100% 固定**だったので、高 DPI 環境では文字が小さいままだった。
  切り替えは即座に反映される（再起動は不要）。
- **拡大率に合わせてウィンドウの大きさも変わる**（1920x1080 × 拡大率。150% なら 2880x1620）。
  作業面積は論理サイズで 1920x1080 のままなので、文字と部品だけが大きくなる。
  ドックの分割幅は ini にピクセルで残るため、切り替えた直後は
  `表示 > レイアウトをリセット` で組み直すと釣り合う。
- 垂直同期・ホットリロード・背景色は情報パネルから設定ウィンドウへ移した。
- `表示 > ウィンドウサイズを既定に戻す` も設定ウィンドウへ移した
  （`設定 > ウィンドウ > 既定の大きさに戻す`）。

### 2026-08-31 22:41 — ウィンドウサイズを 1920x1080 で固定する

- 起動時のクライアント領域が**必ず 1920x1080** になるようにした。
  これまではモニタの作業領域に収まらないと縮めていたため、
  タスクバーのぶんだけ小さいウィンドウで開くことがあった。
  はみ出す場合は縮めず、位置だけ画面内へ寄せる（警告はログに出る）。
- `表示 > ウィンドウサイズを既定に戻す` を追加。
  手で広げたりスナップしたりしたあと、1920x1080 へ戻せる（最大化も解除する）。

### 2026-08-31 22:12 — 最近使ったプロジェクト

- ファイルメニューに **「最近使ったプロジェクト」** を追加した（最大 10 件、新しい順）。
  項目にはファイル名、右の列に置き場所を出すので、同じ名前でも見分けられる。
- 履歴は `%LOCALAPPDATA%/material-mixer/recent.json` に置く。
  プロジェクトの中身ではなくアプリ側の状態なので、`.mmproj` には入れない。
- 開けなかったプロジェクト（消した / 壊れた）は履歴から自動で外れる。
- 「履歴を消す」でまとめて消せる。

### 2026-08-31 21:36 — ファイル操作のショートカット

- `Ctrl + S` で保存できるようにした。保存先が決まっていなければ保存先を聞く。
- ほかに `Ctrl + N`（新規）、`Ctrl + O`（開く）、`Ctrl + Shift + S`（名前を付けて保存）。
- メニューにも割り当てを併記した。

### 2026-08-31 21:24 — 画角を焦点距離 (mm) で指定する

- プレビュー設定のカメラを、**ラジアンの「画角」から焦点距離 (mm)** に変えた。
  露出を絞り / シャッター / ISO で決めているので、レンズも同じ言葉に揃える。
  換算は 35mm フルサイズ（センサー高 24mm）で、縦画角を基準にする。
- 結果の画角は度で併記する（表示専用）。既定は 29mm = 45 度で、これまでと同じ見え方。

### 2026-08-31 15:49 — ステータスバー、ファイルのドロップ、EXR 表示の修正

- **画面下端にステータスバーを追加した。**
  左に直近の通知（保存した、読み込めなかった、など）と、いまのモード。
  右にプロジェクト名・レイヤー / マテリアル / テクスチャの数・合成解像度・FPS。
  **これまで警告やエラーはデバッグ出力にしか出ておらず、
  読み込みが成功したのかどうか画面からは分からなかった。**
- **エクスプローラからファイルをドロップできるようにした。**
  拡張子で行き先が決まる（画像 → テクスチャ / `.mmproj` → プロジェクト /
  `.mmmat` → マテリアル / `.hdr` → 環境）。扱えない形式は理由を出す。
- **EXR のサムネイルが真っ暗に近かったのを直した。**
  EXR の中身はリニアで、ImGui はその値をそのまま描くため極端に暗くなっていた。
  読み込み時に sRGB へ焼いた表示用テクスチャを作るようにした。
- 読み込んだテクスチャを選択して一覧に見せるようにした
  （一覧はスクロールするので、末尾に足すだけでは気づけなかった）。

### 2026-08-31 15:12 — M5b: テクスチャ一覧と、プロジェクト / マテリアルの保存

- **テクスチャパネル**を追加した（レイヤー・マテリアルと同じ枠の 3 つめのタブ）。
  読み込んだ画像をサムネイルで一覧でき、解像度・ミップ数・形式・参照数・置き場所を確認できる。
  名前を付け替えられ、使わなくなった画像は削除できる。
  **サムネイルをマテリアルのマップ欄へドラッグすると割り当たる。**
  画像の読み込み口はマテリアルパネルからこちらへ移した。
- **プロジェクトの保存と読み込み**（`.mmproj`）。
  レイヤー / マテリアル / テクスチャの参照 / ペイントマスク / プレビュー設定を保存する。
  ファイルメニューに 新規 / 開く / 保存 / 名前を付けて保存 / 終了 を追加。
  タイトルバーに開いているプロジェクト名が出る。
  - **マテリアルの構造はプロジェクトへ丸ごと埋め込む。** 開くのに別ファイルは要らない。
  - 画像は参照だけを持ち、パスはプロジェクトからの相対で記録する。
    プロジェクトごと移動しても壊れない。
  - ペイントマスクは手続きで再現できないので、`<名前>.assets/` へ PNG で書き出す。
- **マテリアル単体の書き出しと読み込み**（`.mmmat`）。
  マテリアルパネルの「書き出し…」「読み込み…」から、プロジェクト間で持ち回れる。
  読み込むと参照している画像も自動で読み込まれ、既に持っている画像は読み直さない。
- 形式は JSON で、値は名前で書いてある（[docs/reference/file-format.md](reference/file-format.md)）。
  手で開いて読める。壊れたファイルは断り、画像が見つからないときは
  その割り当てだけ「なし」にして残りを読む。
- 起動オプション `--project <path>` を追加（起動時にプロジェクトを開く）。

### 2026-08-31 18:52 — プロジェクト名を material-mixer に変更

- 名前を `heightmap-mixer` から **`material-mixer`** に変えた。
  扱っているのは BaseColor / Normal / Surface / Height の 4 チャンネルを持つマテリアルで、
  ハイトマップはその 1 チャンネルとハイトブレンドの駆動値にすぎない。
  実行ファイルは `material_mixer.exe`、ウィンドウのタイトルは `Material Mixer`。
- 内部の識別子もそろえた。`namespace hm` → `namespace mm`、`HM_*` → `MM_*`
  （C++ のマクロ、HLSL の定数、インクルードガード）。
- レイアウトの保存先が `material_mixer_imgui.ini` に変わるため、
  パネル配置は一度既定に戻る。

### 2026-08-31 18:04 — チャンネルパックされたテクスチャと EXR に対応

- **Megascans の `_ORD` のように、1 枚に複数のマップを詰めたテクスチャを使えるようにした。**
  ラフネス / メタルネス / AO / ハイトは「テクスチャ + どのチャンネルを読むか」で指定する。
  マスク用テクスチャも同じ。
- マテリアルパネルに `ORD` 行を追加。1 枚を選んで「割り当て」を押すと、
  AO=R / ラフネス=G / ハイト=B にまとめて割り当てる（Megascans の並び）。
  個別に選び直せるので、並びが違う素材（UE の ORM など）にも対応できる。
- **EXR (.exr) の読み込みに対応した。** 16bit float のまま持つので、
  ハイトに 8bit の階段が出ない。Megascans のテクスチャをそのまま使える。

### 2026-08-31 17:12 — M5a: マテリアルライブラリ（サムネイル付き）

- **マテリアル**を追加した。PBR のマップ一式（ベースカラー / 法線 / ラフネス /
  メタルネス / AO / ハイト）に名前を付けたもので、**レイヤーはこれを 1 つ参照する**。
  マップを 1 枚ずつ差し替えるのではなく、マテリアルを差し替えて見た目を変える。
- **マテリアルパネル**を追加（レイヤーのとなりのタブ）。
  サムネイルの一覧から選び、マップとパラメータを編集できる。
- サムネイルは球にマテリアルを当てて描く。一覧で見比べられる。
- **テクスチャの読み込みがファイル選択ダイアログになった。** 複数まとめて選べる。
  HDRI も同じくダイアログから開く。パスの手入力欄は廃止。
- マテリアルを割り当てたレイヤーでは、色とサーフェスの値はマテリアル側で決まる。
  レイヤー側の同じ項目は隠れる（同じ意味の値が 2 か所にあると分からなくなるため）。
- マスク用のテクスチャはレイヤー側に残した。マスクは「どこに出すか」を決めるもので、
  マテリアルの「何を出すか」とは役割が違う。

### 2026-08-31 16:02 — パネルをドックに収め、レイアウトのリセットを追加

- **右のパネルが画面外へはみ出して見切れる問題を修正した。**
  パネルを絶対座標で置いていたため、ウィンドウの大きさが変わったときや、
  別の大きさで保存されたレイアウト(ini)を読んだときに収まらなくなっていた。
  すべてのパネルをドックへ収める形に変え、ウィンドウの大きさに必ず追従するようにした。
- `表示 > レイアウトをリセット` を追加。パネルの配置を既定へ戻す。

### 2026-08-31 15:24 — 座標系を右手系に統一し、座標軸ギズモを追加

- ビューポートの座標系を**右手系 Y-up** に統一した。X が右、Y が上、Z が手前。
  これまでは左手系の行列を使っていたため画面が左右反転しており、
  ワールドの +X が画面左に出ていた。対称なプリミティブでは気づきにくい状態だった。
- あわせて**ビューポートのドラッグの向きが直った**。
  左ドラッグでもパンでも、ドラッグした向きに内容が付いてくる。
- ビューポートの左下に**座標軸ギズモ**を追加。X = 赤 / Y = 緑 / Z = 青で、
  奥を向いている軸は暗く表示する。

### 2026-08-31 14:36 — UI をグレー基調に整理

- 配色をグレー基調に作り直した。彩度をほぼ持たせず、明度だけで面の階層を作る。
  アクセントは 1 色だけで、チェックや「既定値から変えた印」にしか使わない。
- **すべての設定値を「パラメータ名：値」の 2 列に揃えた。** 見出し・幅・ツールチップも統一。
- 各行の右端に**既定値マーカー**を追加。既定値から変えてある項目が一目で分かり、
  押すと既定値へ戻る。
- スライダーは Ctrl + クリックで数値を直接入力できる（入力欄は別に置かない）。
- **レイヤーの並べ替えをドラッグ＆ドロップにした。**「上へ」「下へ」ボタンは廃止。
- ウィンドウのクライアント領域を 1920x1080 に固定した。
  UI もその面へ 1:1 で載せるので、モニタの表示スケールで作業面積が変わらない。
- 開発用オプション `--screenshot-ui <path>` を追加。
  UI 込みのウィンドウ全体を PNG に書き出す。画面キャプチャに頼らずに見た目を確認できる。
- UI のルールを [docs/design/design-guide.md](design/design-guide.md) にまとめた。

### 2026-08-31 13:32 — M4b: ペイントマスク（ブラシ）

- マスクの出どころに「ペイント」を追加。ビューポート上でブラシを引いて、
  レイヤーのマスクを直接描けるようになった。
  プロシージャルでは狙えない場所（ここだけ苔、この一筆だけ砂）を指定できる。
- ペイントモード中は左ドラッグで塗り、右ドラッグで消す。
  視点を回すときは Alt + 左ドラッグ。中ボタンのパンとホイールのズームはそのまま。
- **ブラシの半径は画面上の見た目で決まる。** 視点を近づけても遠ざけても、
  UV スケールを変えても、カーソルの円の大きさどおりに塗れる。
- ブラシの強さ・減衰、消しゴム、全消去、全塗り。
- アンドゥ / リドゥ（24 段）。単位は 1 ストローク。
- ペイント解像度を 512 / 1024 / 2048 から選べる。
  変更しても描いた内容は拡大縮小して引き継ぐ（履歴は破棄される）。
- レイヤーを複製するとペイントマスクも中身ごと複製する。
  レイヤーを削除するとペイントマスクも破棄する。

### 2026-08-31 12:34 — M4a: マスク生成（中間結果由来のマスク）

- マスクの出どころに「下地の高さ / 傾斜 / 曲率 / 窪み」を追加。
  そのレイヤーより下を合成した結果から自動でマスクを作れる。
  「窪みにだけ苔を生やす」「急斜面にだけ岩を出す」といった指定が書けるようになった。
- ノイズの種類を追加。fBm に加えて尾根状とセル状（Worley）を選べる。
  ハイトとマスクの双方で使える。
- マスクにカーブ（コントラスト）を追加。境界のはっきり具合を調整できる。
- 既定のレイヤー構成に「窪みに苔が生える」レイヤーを追加した。

### 2026-08-31 12:09 — ドキュメント整理

- `README.md` を追加。概要、ビルド手順、実行方法、操作、ディレクトリ構成をまとめた。
- `docs/design/` を新設し、実装の設計ガイドを 3 本に分けた
  （RHI / 描画 / 合成）。

### 2026-08-31 12:02 — M3b: テクスチャレイヤー

- レイヤーのチャンネルに画像を割り当てられるようにした。
  ベースカラー / 法線 / ラフネス / メタルネス / AO / ハイト / マスクの 7 スロット。
- PNG / TGA / JPG の読み込みに対応。レイヤーパネルからパスを指定して読み込める。
- 読み込んだ画像はミップを自動生成するため、縮小して使ってもざらつかない。
- ベースカラーは sRGB、それ以外はリニアとして正しく読む。
  同じ画像をどちらの用途にも使える。
- ハイトとマスクの出どころに「テクスチャ」を追加。
  ハイトテクスチャを使った合成ができるようになった。
- 起動オプション `--texture <path>` を追加（繰り返し指定可）。

### 2026-08-31 11:52 — ビューポート操作の修正

- ビューポートをドラッグするとカメラと同時にパネルまで動いてしまう問題を修正。
- ウィンドウの移動をタイトルバーからだけに限定した。
- ビューポート上のホイールがウィンドウのスクロールに取られないようにした。
- 右ドラッグでも平行移動できるようにした。

### 2026-08-31 11:37 — M3a: レイヤー合成エンジン

- レイヤースタックによるマテリアル合成に対応。
  レイヤーごとにベースカラー・ラフネス・メタルネス・AO・ハイト・マスクを設定でき、
  ハイトベースブレンドで積み重ねられる。
- ハイトとマスクの出どころとしてフラクタルノイズを用意。
  法線はハイトの勾配から自動的に作られる。
- マスクのレベル調整と反転に対応。
- レイヤーが書き込むチャンネル（BaseColor / Normal / Surface / Height）を個別に切り替えられる。
- 合成結果をプレビューのメッシュに貼れるようにした。合成解像度は 512 / 1024 / 2048 から選べる。
- レイヤーパネルを追加（追加 / 複製 / 削除 / 並べ替え / 有効切り替え）。
- 既定のレイヤー構成を「岩の隙間に砂が溜まる」2 レイヤーにした。

### 2026-08-31 11:13 — M2b: IBL（環境光）

- 環境マップによるライティング（IBL）に対応。
  拡散は irradiance キューブ、鏡面はラフネス別にプリフィルタしたキューブと
  環境 BRDF の LUT を使う分割和近似。
- Radiance HDR (.hdr) の読み込みに対応。UI からパスを指定して読み込める。
- HDRI を読み込んでいないときは手続き的な空を使う。
  天頂色・地平色・地面色・輝度を UI から調整でき、即座に環境が作り直される。
- 背景（スカイボックス）の表示に対応。
- 環境光の強さを UI から調整できるようにした。
- 開発用のコマンドラインオプションを追加。
  `--hdri <path>` で起動時に HDRI を読み込み、
  `--screenshot <path>` でビューポートを PNG に書き出して終了する。

### 2026-08-31 10:46 — M2a: PBR プレビューレンダラ（直接光まで）

- 球 / 平面 / キューブを GGX 直接光で描画するプレビューを追加。
  ベースカラー、ラフネス、メタルネスを UI から調整できる。
- 物理カメラベースの露出に対応。絞り / シャッター / ISO から EV100 を求める方式と、
  EV を直接指定する方式を切り替えられる。
- トーンマップを追加（なし / Reinhard / ACES）。
- ディレクショナルライトの方位角・仰角・照度（lux）・色を調整できるようにした。
- 軌道カメラを追加。ビューポート上で左ドラッグ回転、中ドラッグ平行移動、ホイールでズーム。
- 高 DPI ディスプレイに対応。UI が拡大されず、描画も等倍解像度で行われるようになった。
- ウィンドウが画面からはみ出さないよう、起動時にモニタの作業領域へ収めるようにした。

### 2026-08-31 10:12 — M1: RHI の整備

- D3D12MemoryAllocator を導入し、テクスチャ / バッファの生成とディスクリプタ確保を集約。
- フレームごとに巻き戻るアップロードリングバッファを追加（既定 16 MB/フレーム）。
- GPU 参照中のリソースをフレーム同期後に解放する遅延破棄キューを追加。
- DXC によるシェーダのランタイムコンパイルと、`shaders/` の更新検出によるホットリロードを追加。
- 全パス共通のグローバルルートシグネチャと、コンピュート PSO のキャッシュを追加。
- bindless（SM 6.6 `ResourceDescriptorHeap`）を全面採用。
  起動時に Resource Binding Tier 3 を要求する。
- 疎通確認用のコンピュートシェーダ（fBm + 傾斜マスク）と、その結果を映すプレビューウィンドウを追加。
- `USE_PIX` を常時定義し、Release でも PIX マーカーが有効になるようにした。

### 2026-08-31 06:18 — M0: 基盤の立ち上げ

- CMake + vcpkg（manifest モード）によるプロジェクト構成を追加。
  構成プリセット `x64`、ビルドプリセット `x64-debug` / `x64-release`。
- Win32 ウィンドウと DirectX 12 のデバイス、コマンドキュー、スワップチェーン、
  フレーム同期を実装。
- RTV / SRV 用のディスクリプタアロケータを追加。
- Dear ImGui（docking ブランチ）を統合。日本語フォントの読み込みに対応。
- Debug ビルドでデバッグレイヤーと GPU ベースバリデーションを有効化。
  フレームに PIX マーカーを追加。
- DirectX 12 Agility SDK の DLL をポストビルドで `D3D12/` へ配置。
